import { cssInterop } from "nativewind";
import { Tabs } from "expo-router";

// Tabs might need specific handling if className is used on it,
// or if basic props need to be mapped.
// For now, we leave it empty or map style if needed.
// If the error persists, we might need to wrap Tabs.
